# Capillary-pair PRL package

## Manuscripts

- Main Letter: rotating_colloids_prl_capillary.tex
- Compiled Letter: rotating_colloids_prl_capillary.pdf
- Supplemental Material: rotating_colloids_prl_capillary_supplement.tex
- Compiled Supplemental Material: rotating_colloids_prl_capillary_supplement.pdf

The earlier pair and grooved manuscripts were not overwritten.

## Reproducible analysis

- Simulator: ../../scripts/rotating_colloids_capillary_pair.py
- Spatial-correlation analysis: ../../scripts/analyze_rotating_colloids_capillary_internal.py
- Finite-size phase classifier: ../../scripts/classify_rotating_colloids_capillary_regimes.py
- Figure builder: ../../scripts/plot_rotating_colloids_capillary_prl.py
- Order-of-operations scan: ../../scripts/test_rotating_colloids_operation_order_memory.py
- Order-of-operations figure: ../../scripts/plot_rotating_colloids_operation_order_memory.py
- Relaxed-exchange minimal model: ../../scripts/test_relaxed_exchange_order.py
- Time-step convergence: ../../scripts/validate_rotating_colloids_timestep.py
- Mobile-cage validation: ../../scripts/validate_rotating_colloids_mobile_cage.py
- Validation merger: ../../scripts/merge_rotating_colloids_submission_validations.py
- Four-GPU submission validation: ../../scripts/run_rotating_colloids_prl_submission_validations_4gpu.sh
- Claim audit: ../../scripts/audit_rotating_colloids_capillary_prl.py
- Unit tests: ../../tests/test_rotating_colloids_capillary_pair.py
- Relaxed-exchange tests: ../../tests/test_relaxed_exchange_order.py
- Publication-scale GPU run: ../../scripts/run_rotating_colloids_capillary_pair_prl_gpu.sh

## Current evidence level

The completed local calculation supports a finite-size, field-free
orientational-memory regime generated by pair interactions on a caged
disordered graph. It does not establish a thermodynamic glass transition.
The legacy numerical values are reconstructed by 110 passing claim checks.
The audit expands to 126 checks when the completed submission-validation
directory is installed; on this host it reports 110/111 because that cluster
directory has not yet been transferred.

The two-pulse calculation additionally establishes a signed sequence readout
after both fields are removed. With independent Brownian histories at
\(N=256\) and \(h=8k_{\rm B}T\), the contested support retains
\(M_{\rm ord}=0.6336\pm0.0198\), versus \(0.0123\pm0.0027\) for separated
supports. The pulse order is decoded with 100\% and \(55.0\pm1.3\%\)
accuracy, respectively. A coupled double-well control
shows why the pulse commutator alone cannot define memory: its full-support
bracket is largest where relaxation erases the AB/BA difference. The retained
observable is therefore defined after quotienting the driven states by the
field-free relaxation map.

The numerical and provenance gates are separate. The cluster-generated
`activated_memory_scan.jsonl` shards are now installed, the audit's
`raw_reproduces_derived_report` gate passes, and Fig. 4 has been rebuilt from
them with no change to any published value (`max_relative_change` 0.0). The
figure reports the endpoint retained overlap at the common observation time.
Identical raw-row duplicates are removed by stable record key; conflicting
duplicates abort the rebuild.

The positional-disorder retention values also pass their numerical checks,
but the local summary records that the underlying `N=576` and `N=1024`
write--release trajectories have not yet been installed. Consequently the
publication-level provenance gate is false even though the quantitative audit
passes.

## Resolved: Letter length

`scripts/count_prl_length.py` estimates the Letter at 3718 APS core word
equivalents against the 3750-word PRL limit: body 2518, captions 183, four
full-width figures 937, and displayed equations 80. The abstract,
acknowledgments, byline, and references are excluded as required by APS. All
four main figures and the full mechanistic, dynamical, finite-size,
sequence-memory, and experimental interpretation remain in the Letter;
numerical protocol detail is retained in the Supplemental Material.

## Completed submission validations

The discriminating tests are resumable and sharded across four
GPUs: weak time-step convergence at equal physical duration, retention when
the particle centres undergo caged translational Brownian motion, and
two-pulse decoding with independent thermal noise. Their outputs are merged
with conflict detection and a predeclared mobile-cage gate. The completed
cluster run contains 15 time-step rows, 25 mobile-cage rows, and 50
independent-noise sequence rows. Its aggregate results are reported in the
Supplement; the complete validation directory must be present when building
the archival release.

Run from the repository root on the four-GPU machine:

    nohup bash scripts/run_rotating_colloids_prl_submission_validations_4gpu.sh \
      > rotating_colloids_prl_submission_validations.log 2>&1 < /dev/null &

The runner is resumable. Rerunning it skips complete JSONL records and rebuilds
the merged reports.

## Restore the disorder-retention trajectories

The local Zenodo staging tree remains incomplete until the 33 raw
write--release cells behind the positional-disorder result are restored. The
four-GPU driver reproduces the exact production schedule used by the quoted
(D_rt=624.5) endpoints. It validates and skips complete cells, so a restart
reruns only an interrupted graph:

    nohup bash scripts/run_rotating_colloids_disorder_retention_4gpu.sh \
      > rotating_colloids_disorder_retention_4gpu.log 2>&1 < /dev/null &

After completion, rebuild the Zenodo tree with:

    python -B scripts/build_rotating_colloids_release.py \
      --disorder-retention-input discoveries/theory_experiment_interface/rotating_colloids_hyperion/rotating_colloids_disorder_retention_protocols \
      --output-dir release/zenodo_geometry_encoded_orientational_memory \
      --clean

## Resolved: Fig. 4(b) window censoring

Panel (b) previously reported the finite-window integrated overlap
`A_Q = int_0^{T_obs} max[Q(t),0] dt / |Q(0)|`. Because `Q(t) <= Q(0)`, the
quantity `D_r A_Q` is bounded above by the observation window (625), so the
physical branch saturated against that ceiling at large coupling (74% of it at
lambda = 1.4) while the `g = 0` branch continued to grow. The enhancement ratio
therefore peaked near lambda = 1.05 and fell afterwards for an instrumental
reason rather than a physical one.

The panel now reports the endpoint retained overlap `Q(T_obs)`, which has no
such ceiling. It rises monotonically from 0.001 to 0.691 across the scanned
range, while the matched `g = 0` control is consistent with zero at every
coupling. Retention is stated as a separation from the null control in units of
the combined standard error (3.4 sigma at lambda = 0.60, rising to 35-45 sigma
for lambda >= 0.90) rather than as a ratio: recomputing the old ratio on the
endpoint measure returns -169 and -55, sign-flipped by a denominator consistent
with zero, which is what made it unstable.

The two audit checks that froze the old ratios were replaced by separation
checks, and the provenance gate now recomputes the endpoint measure from the
raw shards. The audit passes 110 of 110 with `raw_reproduces_derived_report`
true.

## Release objects

- Private code repository: `vbaulin/geometry-encoded-orientational-memory`
- Zenodo staging tree: `release/zenodo_geometry_encoded_orientational_memory/`
- Data builder: `../../scripts/build_rotating_colloids_release.py`
- Resumable Zenodo uploader: `../../scripts/upload_rotating_colloids_zenodo.py`

The data builder computes SHA-256 hashes, excludes manuscript figures and
PDFs, deduplicates activated-memory shards by content hash, stages the order-
of-operations and submission-validation datasets, and refuses a complete
build when any raw source cited by the manuscript is absent.

To create a new Zenodo draft from either the cluster or this machine, first
build a complete release and run:

    python3 -B scripts/upload_rotating_colloids_zenodo.py \
      --release-dir release/zenodo_geometry_encoded_orientational_memory \
      --env-file .env --create

The command packages the release into one `tar.gz`, uploads the archive and
the five human-readable manifest files, and writes a hidden state file beside
the release. Repeating it resumes the same draft and skips matching remote
files. Inspect the printed draft URL, then publish exactly once with:

    python3 -B scripts/upload_rotating_colloids_zenodo.py \
      --release-dir release/zenodo_geometry_encoded_orientational_memory \
      --env-file .env --publish

Omit `--create` on the publishing command so an absent state file cannot
silently create another deposition. Use `--dry-run` before either network
operation to verify the archive and print its SHA-256 checksum.

The main figures now show the physical capillary realization, the
finite-size hidden-memory state diagram and crossover response, the
dynamical memory protocols, and the coupling-dependent retention scan. The
positional-disorder scan is Supplemental Fig. S3, the retention maximum is
Supplemental Fig. S4, and matched controls and finite-size diagnostics are
retained as Supplemental Fig. S5.

## Publication-scale run

Run the following command from the repository root on the GPU machine:

    nohup bash scripts/run_rotating_colloids_capillary_pair_prl_gpu.sh \
      > rotating_colloids_capillary_pair_prl_gpu.log 2>&1 < /dev/null &

The run is resumable. It extends the map to \(N=400\), the selected-point
scaling to \(N=2304\), and the dynamic protocols to \(D_rt=625\) on three
independent positional graphs.

For the three-graph dense phase diagram only:

    nohup env CUDA_VISIBLE_DEVICES=0 PHASE_ONLY=1 \
      PHASE_GRAPH_SEEDS=17,29,43 \
      bash scripts/run_rotating_colloids_capillary_pair_prl_gpu.sh \
      > rotating_colloids_capillary_phase_gpu.log 2>&1 < /dev/null &

The phase-only run writes the discrete dynamical phase diagram,
classification-confidence map, state-space clouds, silhouette scores, and
cluster-stability audit under
discoveries/theory_experiment_interface/rotating_colloids_hyperion/rotating_colloids_capillary_pair_prl_gpu/phase_diagram/.

## Rebuild Fig. 4

The eight-coupling retention scan is a separate job. It shards the five
quenched graphs over whatever CUDA devices are visible, so the same command
works on one RTX 3090 and on a four-GPU node:

    nohup bash scripts/run_rotating_colloids_activated_memory_prl_gpu.sh \
      > rotating_colloids_activated_memory_gpu.log 2>&1 < /dev/null &

It writes `activated_memory_scan.jsonl` shards under
`discoveries/theory_experiment_interface/rotating_colloids_hyperion/rotating_colloids_activated_memory_prl_gpu/`
and then rebuilds `fig4_activated_memory_results.pdf` and its report. The scan
is resumable: rerunning skips completed graph/coupling points.

The plot script compares the new report against the previous one and writes
`activated_memory_report_delta.json`. A nonzero `max_relative_change` means
the numbers quoted in the Letter and the frozen expectations in
`scripts/audit_rotating_colloids_capillary_prl.py` must be updated together.
Keep a copy of the current `activated_memory_figure_report.json` before the
rerun so the comparison is meaningful.

Once the raw activated-memory shards and disorder-protocol directories exist,
the audit's provenance block reports complete raw support and the deposit can
be completed:

    python -B scripts/build_rotating_colloids_release.py \
      --root "$PWD" \
      --disorder-retention-input /path/to/disorder_protocol_runs \
      --output-dir "$PWD/release/zenodo_geometry_encoded_orientational_memory" \
      --clean
